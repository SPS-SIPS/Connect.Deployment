apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus_local
    url: http://prometheus:9090
    access: proxy
    isDefault: true
    editable: true

  - name: Loki
    type: loki
    uid: loki_local
    url: http://loki:3100
    access: proxy
    editable: true
